import { UseTestDirective } from './use-test.directive';

describe('UseTestDirective', () => {
  it('should create an instance', () => {
    const directive = new UseTestDirective();
    expect(directive).toBeTruthy();
  });
});
